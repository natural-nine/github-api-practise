import React from "react";
import styled from "styled-components";

const ThemeMode = () => {
  return <ThemeBox></ThemeBox>;
};

const ThemeBox = styled.div`
  width: 15%;
  height: 85%;
  border: 1px solid blue;
`;

export default ThemeMode;
